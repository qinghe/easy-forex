<%= simple_form_for [:account, @exchange] do |f| %>
  <%= f.error_notification %>

  <%= f.input :title %>
  <%= f.input :amount %>
  <%= f.input :pair_currency, prompt: "Select a pair currency" %>
  <%= f.input :city %>

  <%= f.button :submit, "Save", class: 'btn btn-primary' %>
<% end %>
